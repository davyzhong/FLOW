# 客观分析 PDF 格式评审（已定稿）

- 日期：2026-09-07；状态：**用户已完成 8 项格式决策，渲染链路按决策定稿**
- 决策记录见第 2 节；实施与验收证据见第 3 节
- 生成方式：`services/api/.venv/bin/python scripts/generate_objective_report_pdfs.py`
  （本地 Postgres + 固定 Chromium CDP 打印，可复现）
- 视觉验收：四轮评审迭代；终验 17 页逐页核对通过（页脚页码 1→N 连续、
  图表/KPI/摘要/附录四方数值互证、无裁切遮挡）

## 1. 评审样例（定稿版）

| 文件 | 覆盖形态 |
| --- | --- |
| `format-review/2026Q1_顺丰控股_*.pdf`（11 页） | A 股 CAS 季报：全表 + 图表 + 杜邦 + 附录逐行 |
| `format-review/2026Q2_腾讯控股_*.pdf`（6 页） | 港股 IFRS 业绩公告：缺表显式降级（"—"/not_computable/整图隐藏） |

京东物流 FY2025 暂缺席，原因见第 4 节缺陷登记（契约级，不阻塞格式定稿）。

## 2. 格式决策记录（用户 2026-09-07 确认）

| # | 决策点 | 用户选择 | 实施 |
| --- | --- | --- | --- |
| 1 | 量纲总策略 | **自动** | 图表自动选量纲 + 正文表格统一"亿"（块内一致，小值块自动万元）——维持 |
| 2 | 封面单位行 | **改表述** | "源数据单位 人民币千元 · 展示量纲 亿元为主（小值自动万元，每股类为元）" |
| 3 | 每页页脚+页码 | **立项** | 已实现：打印器改为 Chromium DevTools 协议 `Page.printToPDF`（footerTemplate），每页左"公司 期间 · FLOW 客观财务分析引擎"、右"第 X 页 / 共 N 页"；新增直接依赖 `websockets`（复用 uvicorn[standard] 树内已装 17.1，依赖评审记录：架构方向确认、坐标级证据查询不可用，版本锁定由 uv.lock 承担） |
| 4 | 分页策略 | **紧凑连排** | 移除章节强制分页；跨页长表表头重复保留；表格/图块 `page-break-inside: avoid` |
| 5 | 引擎表取整 | **是** | 纯数值 4 位小数、百分数 2 位小数（尾部零省略）；非数值字符串原样 |
| 6 | 涨跌配色 | **中国惯例** | 红涨绿跌——维持 |
| 7 | 资产环形图口径 | **占选列合计** | 现口径（标题已注明"选列主要资产项"）——维持 |
| 8 | 缺表章节 | **保留章节显式** | 缺表章节保留并显式"—"——维持 |

## 3. 定稿验收证据

- 顺丰 11 页页脚页码 1→11、腾讯 6 页 1→6，连续递增，逐页核对正文无遮挡；
- 紧凑连排后新暴露的杜邦图 suptitle 越界裁切已修（savefig 加 `bbox_inches="tight"`）；
- 引擎表值列全部为取整短值（0.0614 / 0.1228 / 0.0238 等）；
- 封面单位行两段式表述在两家公司上正确适配（千元 / 百万元）；
- 回归：statements 54 测试、ruff、mypy 145 文件全绿。

## 4. 本轮发现并登记的缺陷（不阻塞定稿，按计划处置）

| 缺陷 | 影响 | 处置 |
| --- | --- | --- |
| JDL FY2025 归一化 UniqueViolation：港股 IFRS 报表存在合法同名行项目（借款/租赁负债等在流动、非流动分组下各一行），`uq_statement_normalized_item` 唯一键假设"每报表行名唯一"不成立 | 京东物流 PDF 无法生成 | P01/P02 契约决策（唯一键需引入分组/序号维度），不绕过 |
| 腾讯种子利润表缺行：附录"除税前盈利 696.90 亿"与所列项目加总差 111.84 亿，疑缺"其他收益净额"类行 | 腾讯报告附录该段加总不平 | P01/P02 对照披露原文补种子行（D049：缺失不补造，先修数据源） |
| 引擎条目"杜邦三分解"值（净利润总额口径）与正文杜邦因子乘积（归母口径）同名不同值 | 引擎表与第四节数字不一致观感 | P01/P03：引擎条目名与口径标注细化 |

## 5. 后续

渲染链路已定稿为 /reports"导出客观分析 PDF"功能的实现基准；下一集成步骤
（P06/P08 范围）是把该渲染器接到客观报告资格与统一冻结载荷上。
