# 2026-09-04 全量项目文档更新记录

代码核对基线：`c1a59d1`。本任务更新文档与截图，不修改业务代码、数据库迁移、依赖或 CI 脚本。

## 交付

- 重写根 README：产品定位、真实界面、功能状态、架构、血缘、复核、数据契约、指标、启动、认证、升级、分层测试、目录与下一步。
- 新增 `docs/README.md` 文档中心、`docs/api-reference.md`（28 个 FastAPI operation）、`docs/documentation-status.md` 适用性登记。
- 更新运行架构、领域对象、数据契约、Intake、指标、认证六份技术文档。
- 更新知识库状态、Agent 起点、交接、重启提示、变更影响图、视觉入口和决策补充。
- 逐份登记 33 份规格/计划/验收/审查文档，32 份加入当前适用性说明；保留历史测试数据、失败记录与原始 checklist 语境。
- 采集七张真实页面截图：驾驶舱、调查、映射、清洗、冻结报告、登录和移动入口。代码/数据/浏览器/存储替身边界见[截图说明](../assets/screenshots/README.md)。

## 事实更正

已完成的导入页面、报告中心、单用户登录不再列为“尚无实现”；当前迁移头为 `0010_frozen_reports`。同时明确：Intake 发布不自动编排指标和分析；默认 Copilot 为离线确定性提供器；默认 PublicationService 未配置 PDF printer；Celery 示例任务尚未真正执行业务；开发 Compose 不等同于生产部署。

原始会话、研究原文、原始图片、批准规格归档快照及外部知识库保持原状，不为追求“全部最新”改写历史。当前权威入口与历史背景通过索引分开。

## 验证

- 独立技术复核核对启动步骤、版本、十张工作表、API 路径和功能边界；新读者检查覆盖定位、启动、上传后计算、AI/PDF、认证、缺口及历史证据。
- 据复核修正了报告资格图：`rejected` 是终态，不能画作可重新提交；API 状态列明确只是 OpenAPI 声明，并非运行时错误穷举。
- 全部 17 个 Mermaid 块通过解析；README 六张图在 Chrome 实际渲染，七张图片全部成功加载，无预览页面异常。长链图调整为分阶段布局以保持可读性。
- 在独立演示 PostgreSQL 库运行迁移至 0010 和演示初始化，真实浏览器操作登录、驾驶舱进入调查、文件上传、映射确认、清洗结果加载、报告冻结（201）；捕获 pageerror 为空。仅导入源文件存储使用测试适配器。
- 检查 53 份变更 Markdown、268 个本地链接，均无断链；22 个 README Makefile 目标存在，28 个 API operation 与已提交合约一致；知识库 144 条 SHA-256 逐一核对通过。Markdown 围栏、图片引用及 diff 检查通过；原始档案未改动。
- 未因纯文档变更重跑全部业务测试，也不把上一任务的测试数量改写为本任务结果。

## 远端 CI 补充事实

核对 [`c1a59d1` 的 FLOW CI](https://github.com/davyzhong/FLOW/actions/runs/33888190706) 时，多个独立门禁已成功，但 `user-closure-e2e` 失败。其 job 日志在 2026-09-04 15:20:48 UTC 显示 `Another next dev server is already running`，组合脚本前一子门禁的 Next 进程未退出，后续浏览器报 `ERR_CONNECTION_REFUSED`。本文记录该待修复问题，不将其归因于文档，也不在本任务中改业务/验收脚本。记录时 integration 尚未结束；实时状态以运行链接为准。

## 档案与提交范围

知识库衍生内容修改后重新生成 `99_manifest/inventory.tsv` 和 `sha256sums.txt`。根目录七个未跟踪用户原始文件仍不纳入任务；不覆盖外部 Obsidian/DavyBase 内容。文档和截图经检查后统一提交并推送规范远端。
