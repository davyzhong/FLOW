# FLOW 下一阶段升级改造执行准备计划｜2026-09-07

> **2026-09-07 起执行队列由 [统一执行计划](2026-09-07-unified-next-plan.md)（U 系列）单一承载**：本文（P 系列）任务书细节仍有效，勾选时与 U 系列双写映射。

> **给后续执行者**：仅在用户另行明确启动后，使用 executing-plans 逐项执行；行为变更先写失败测试，完成前做 verification-before-completion。每个工作包内部按测试、最小改动、复验、任务范围提交拆成小步。本次不进入执行流程。

**Goal:** 基于累计参考资料和已实现底座，完成可信的公开财报客观分析与报告交付；优先订正语义、补齐独立验证和完整用户路径，再为内部经营分析建立可控扩展条件。

**Architecture:** 延续 Next.js / FastAPI / PostgreSQL 分层；版本化事实与指标合同是唯一计算来源，主题、图表、报告共享不可变快照。会计知识不承担记账，客观分析不依赖主观 Finding，内部数据后续通过共同合同接入。

**Tech Stack:** 现有 TypeScript、Pydantic、SQLAlchemy、Alembic、Decimal、Celery/Redis、S3/MinIO、Playwright；不为本轮引入替代技术栈。实际 PDF 打印能力列为明确交付，不能用假文件或测试替身验收。

建立/更新：2026-09-07。状态：**下一阶段首读详细计划，准备执行；本次仅更新文档，不授权实施。** P00–P12 的复选框表示本计划增量及验收是否交付，不意味着 A/B/C 旧能力尚未实现。

## 1. 依据、约束与优先级

主要输入：[外部借鉴、参考资料与优化订正总册](../../knowledge-base/02_research/synthesis/2026-09-07-reference-and-improvement-master.md)，下文 S、C、I 编号均来自该册。正式约束：[D049 方向规格](../specs/2026-09-06-objective-financial-analysis-direction.md)、[财务事实合同](../specs/financial-facts-contract.md)、[指标库规格](../specs/2026-09-05-flow-metric-dictionary-design.md)、[变更影响图](../../knowledge-base/04_decisions/CHANGE_IMPACT_MAP.md)。

本计划作为下一阶段详细任务入口；[A01–H01 里程碑台账](2026-09-06-objective-financial-analysis-master-plan.md)保留阶段出口与历史证据。本计划细化后续增量和验收，不替换已批准方向，不重新打开已完成 A/B/C 全部工作，不撤销历史验收。开始执行须有用户后续明确指令；届时先做 P00，发现规格冲突先提交决策，不默默实施。

优先级：P0 正确性、可追溯性和独立验证；P1 用户阅读、报告和交付链路；P2 内部数据准备；P3 预测、因果与行动闭环。本次不写程序、不改指标配置、不跑迁移、不部署，也不触发新的持续开发任务。

### 1.1 基线与已具备资产

本轮更新读取基线 `22061da`（含 D01 `c009823` 与文档治理轮 `de3a672`），迁移到 0018。A/B 财务事实链和 C01–C06 指标管理已有连续实现记录，包含执行绑定、语义、版本治理、影响沙箱和用户界面。保留并复用 55 财务指标、167 科目、48 准则登记、32 分录模板及经营轨配置，不能将登记数量解释为可执行覆盖。

已发现差异：`ocf_net_profit_ratio` 已存在，原 U3 不应再次新增同义指标；FCF 的 IFRS 18 MPM 标记需订正；部分验证答案仍为占位/量级，圆通已用于适配；入口状态较旧，不能据其标题认定全部尚未实现。详见总册 C01–C18。

基线 `8c7dbf3`、`0554dca` 的 CI 已成功；D01 已随 `c009823` 提交（`config/analysis/objective_finance_v1.yaml` 11 条目、`analysis/objective.py`、4 项测试），其与后续文档提交的 CI 结果以远端为准。P00 必须核验和复用 D01 已提交结果，不创建平行分析目录。实时状态见 PROJECT_STATE。

### 1.2 下一阶段成功的用户结果

用户可以上传约定范围的真实公开财报，确认主体/期间/币种及映射，解决质量和复核问题，得到四问组织下的客观分析；任一数值都能查看口径与原始证据；不可算有原因，不能被 0 或演示值掩盖；复核后可冻结并下载数值一致的报告；后续数据、词典和组织变化不改写旧报告。

试点边界首先是已登记并经独立验收的企业、报告类型和期间，不承诺“任何 PDF 自动分析”。无法正确解析的输入应进入人工复核/不支持分支而不是假成功。

## 2. 里程碑、依赖与估算

```text
P00 基线 + 先锁独立答案/留出
 ├─ P01 口径订正 → P02 主题/比较合同 → P03 拆解与联动规则
 └─ P04 来源与快照投影（合同随 P01/P02 对齐）
       P01/P03/P04 → P05 独立全行验证与留出验收
       P02/P04 → P07 分析工作台
       P04 → P06 客观报告门禁 → P08 统一冻结与渲染
P05/P07/P08 → P09 完整旅程回归
P08/P09 → P10 发布运行保障 → P11 阶段总验收
P11 通过且另获授权 → P12 内部试点；预测/因果/行动再单独立项
```

独立答案的准备不等到界面做完才开始；必须在接触候选留出样本调参前冻结。P04 可先做来源与身份增量，依赖 P02 的图形合同部分稍后合并。并行仅在文件边界和负责人明确时开展，共享工作树不可交叉覆盖。

| 里程碑 | 任务 | 计划投入（人日） | 出口 |
|---|---|---:|---|
| M-A 定义和验收基线 | P00–P02 | 6–9 | 差异账本、订正口径、独立答案准备、主题合同 |
| M-B 可信分析 | P03–P06 | 12–19 | 确定性拆解、同快照证据、独立验收、事实发布门禁 |
| M-C 可使用交付 | P07–P09 | 9–14 | 用户工作台、多格式、真实存储完整旅程 |
| M-D 发布候选 | P10–P11 | 5–8 | 恢复/安全/可运行证据与阶段结论 |
| 后续单独试点 | P12 | 不计入本阶段 | 数据授权与范围确认后另估算 |

合计 32–50 人日为初始规划量，不是交付日期承诺；P00 后根据已有实现可复用率重估。另建议预留 20% 验证和返工缓冲（约 6–10 人日）。两名开发与一名兼职财务复核者可以部分并行，但财报独立录入、授权与部署资源等待不应算成开发已完成。单人执行不能自称实现者与独立答案录入者互相独立。

## 3. 逐项任务书

所有路径相对仓库根；“新增”是未来执行建议，不表示本次已创建。每任务采用先写反例/验收样本，再实现最小增量，再验证和保存证据的顺序。若 P00 证明已满足，只提交证据和必要回归，不为完成任务而重写。

### P00：校准工程基线与独立验收准备（2–3 人日，P0）

来源 I01/I03/I30；对应旧 A01/A03、D03/D04 的前置部分。负责人：技术负责人 + 独立财务复核者。

文件：读取现有总计划、`docs/implementation/objective-analysis/2026-09-06-baseline-audit.md`、`validation/financial_reports/manifest.yaml`；未来更新 `docs/knowledge-base/00_start_here/PROJECT_STATE.md`；新增 `docs/implementation/objective-analysis/next-stage-baseline.md`、`validation/financial_reports/oracle-register.md`。

- [ ] 记录执行时 HEAD、分支、迁移头、工作树、最新 CI 与 A/B/C 实现证据。列“已验收/已实现待补验/缺口/未知”，解决旧复选框冲突，保留历史记录。
- [ ] 对 I01–I30 和 C01–C18 逐条标记已有实现、新增差异、回归路径和负责人；尤其确认净现比和费用率是否定义/绑定/显示。
- [ ] 保留至少三家基线样本（顺丰、腾讯、京东物流）并盘点开发使用史；圆通等已用于适配者移为回归集，不再作为首次泛化证据。另选至少两份未参与当前适配的留出报告，组合必须包含一家新公司和一份新期间，严格沿用 D049 门槛，不以仅换布局替代。
- [ ] 独立录入者从原 PDF 建逐行标准答案，保存页/表/行、单位、主体、期间、复核者及答案文件哈希；禁止从解析输出或同一公式实现反向生成答案。删除占位的方式是新增精确版本，不改原始 PDF。
- [ ] 将候选留出原件/答案冻结并隔离；记录何时可被实现者看到。若失败后用于调参，降为回归并补充新留出；明确人工介入次数。

验收：全部在范围样本无“同上/补录/量级”作为预期值；差异覆盖 I/C 全量；明确所有未获得的证据。未取得独立录入者或合格留出时可继续非验收性开发，但 P05 和公开准确性结论不得通过。

### P01：指标来源与语义订正（2–3 人日，P0）

来源 I01/I04/I05/I08/I11，订正 C01–C05/C18；对应 C01–C06 的增量，不重建目录。负责人：财务口径负责人 + 后端。

文件：`config/metrics/metric_dictionary_v1.yaml`、`services/api/src/flow_api/metric_library_store/{binding,governance,importer}.py`、`services/api/src/flow_api/metrics/financial_semantics.py`；测试 `services/api/tests/metrics/test_financial_semantics.py`、`services/api/tests/integration/test_metric_library_governance.py`；更新 `docs/implementation/objective-analysis/2026-09-06-caliber-audit.md`。

- [ ] 先写失败测试：现金流 FCF/单独比率不得因“管理常用”自动获得 IFRS MPM 标签；负净利润时净现比不得显示为正常健康评分；别名搜索返回同一指标身份。
- [ ] 逐项补来源机构、文档标题、版本/生效信息、具体条款/页码、适用主体和核验状态。分开“企业管理口径”和“监管定义 MPM”，不得以布尔替换静默改掉所有历史语义。
- [ ] 对 FCF/EBITDA 及比率、营业利润、净债务、调整后利润建立逐项判定与调节要求；有疑问的条目保持待核验，不宣称官方认证。
- [ ] 复用 `ocf_net_profit_ratio`，检查单位倍/百分比显示；费用率先盘点分子科目及口径，再决定是否增一条定义或只增加派生展示。
- [ ] 通过既有草稿、影响分析、激活流程发布新版本；保留历史版本和 YAML 兼容。若新增分类字段确需迁移，先评审前向默认值与历史 unknown 状态，不伪造历史已核验。

验收：每条订正关联官方或明确非权威来源；新版本结果可追到旧版差异；旧快照结果不漂移；词典、执行绑定、UI 元数据一致。禁止仅改文案而计算仍用错误口径。

### P02：主题、指标可用性与比较合同（2–3 人日，P0/P1）

来源 I06/I07/I13–I15，订正 C09/C11/C16/C17；对应 D01。负责人：产品/财务 + 后端。

文件：读取 `services/api/src/flow_api/metrics/{catalog,grain,windows,comparisons}.py`；优先复用 A–H 台账的 `config/analysis/objective_finance_v1.yaml`、`services/api/src/flow_api/analysis/objective.py`、`services/api/tests/analysis/test_objective_finance.py`（已提交 `c009823`，复用前先核验测试与 CI，不另建重复目录）；按现有机制扩展 `services/api/src/flow_api/api/schemas/dashboard.py` 和生成合同。

- [ ] 先定义四问与六专题的多对多映射；现金、偿债横向关联。列默认十指标选择及每项替代/不可用条件，不删除大词典。
- [ ] 每个主题定义 metric/version、必要事实、允许维度、粒度、期间窗口、比较场景、图表类型与证据要求。新对象至少包含 `topic_id`、`metric_refs`、`required_facts`、`allowed_grains`、`comparison_modes`、`unavailable_reasons`。
- [ ] 分开定义已登记、可执行、源数据覆盖、展示启用四种覆盖率；输出逐指标覆盖表，避免只有总数。
- [ ] 比较合同验证币种单位、合并范围、期间、重述版本、存量流量和预算版本。不可比返回有原因的状态，不返回 0 或静默退回其他期间。
- [ ] 测试公开报告有账龄附注时允许对应披露层，无附注时禁用；无客户事实禁用客户下钻；月度预算对订单粒度禁止直接重复联接；零/负预算不输出误导完成率。

验收：四问均有可用或解释性空状态；六专题不是六张假数据图；预算/同行/情景只有前提具备才可选。合同快照测试包含成功、缺失和不兼容输入。

### P03：确定性拆解、联动提示和边界测试（4–6 人日，P0）

来源 I05/I08–I12/I15/I16；订正 C03–C08/C14/C16–C18；对应 D01/D02 增量。负责人：计算后端 + 财务复核者。

文件：`services/api/src/flow_api/metrics/{formulas,comparisons,aggregation,financial_semantics}.py`、`services/api/src/flow_api/analysis/{bridges,playbooks}.py`；测试 `services/api/tests/metrics/test_financial_semantics.py`、`services/api/tests/metrics/test_comparisons.py`；新增 `services/api/tests/analysis/test_objective_signals.py`。

- [ ] 先写已知答案与反例：负/零净利润、负/接近零权益、比例百分点、平均与期末余额、季报累计差分、CAGR 区间、重述期和并购范围变化。累计差分在本计划中先作为拒绝性反例：现行事实合同只允许同 kind 流量相加，未获正式合同修订与决策确认前不得新增差分转换。
- [ ] 应收与收入增速联看输出“事实 + 提示 + 限制 + 证据”，不输出确定坏账/造假原因。ROE 提示分别展示利润和权益变化，指标正常也不自动结论经营优良。
- [ ] 对利润形成区分法定营业利润、管理经营利润及调整项，缺行项目则无法完成相应桥；不通过“其他”吞掉无法解释的解析错误。
- [ ] 量价示例采用明示的路径规则：`数量影响=(Q1−Q0)×P0`，`价格影响=Q1×(P1−P0)`，两者还原收入差额。这是顺序拆解而非唯一因果解释；组合/客户路径另图，不与该桥重复相加。
- [ ] 无业务量或价格事实时只做有披露数据的结构分析。毛利率结构拆解写明加权及交互项处理；残差展示并超过规定舍入容差时阻断。
- [ ] 比率分母不适用返回结构化原因；法规/企业阈值、方向和适用行业必须带版本，不硬编码宣传样板阈值。

验收：确定性重算一致；拆解满足守恒；每个信号可追证据且标记非因果；所有反例通过。公式单元测试和独立答案采用不同实现路径，禁止两边引用同一计算函数。

### P04：同源证据与快照分析投影（3–5 人日，P0）

来源 I02/I16/I17；对应 D02，复用 B 链。负责人：后端。

文件：`services/api/src/flow_api/statements/{extraction,normalization,repository,review}.py`、`services/api/src/flow_api/metrics/{persistence,service}.py`、`services/api/src/flow_api/dashboard/{models,service,repositories}.py`；测试 `services/api/tests/integration/test_dashboard_snapshot_series.py`、`services/api/tests/integration/test_metric_snapshot_atomicity.py`。

- [ ] 先测试跨批次、跨主体、不同币种/合并范围、历史词典版本和当前版本混用时被拒绝；图表与证据必须共享快照身份。
- [ ] 保留原件哈希、定位、人工修正前后值和理由；清洗记录排除行、小计判定及金额校核，不覆盖原值。
- [ ] 建主题读取投影：返回预计算值、比较值、单位、口径版本、来源定位、可用性、复核状态；前端只格式化，不重算。
- [ ] 实现允许维度下钻及粒度校验；合计与明细相加有约束；源未披露的维度不得由模型生成。
- [ ] 验证重复请求幂等、计算失败原子性、旧批次重读与修正后新快照身份，保留既有任务失败恢复路径。

验收：同一快照的页面、API、证据数值一致；任何跨身份拼接都有失败测试；原件不可变；修正可审计；尚不可定位的行不可冒充已复核。

### P05：独立全行验证与留出泛化验收（3–5 人日，P0）

来源 I03；对应 D03/D04。依赖 P00 答案冻结、P01/P03/P04。负责人：独立财务复核者主签，开发修复不得自行替代签字。

文件：`validation/financial_reports/manifest.yaml`；新增 `validation/financial_reports/expected/`、`validation/financial_reports/results/` 和 `scripts/validate_financial_reports.py`；复用 `services/api/tests/statements/` 与 `services/api/tests/metrics/test_metric_oracle.py`。

- [ ] 运行前检查答案与源文件哈希、样本污染状态和复核签名。建立所有范围内报表行的期望集合，不只验收入/净利润几个指标。
- [ ] 差异输出缺行、多行、错位、符号、单位、主体/期间、重复、小计和金额；每行状态可追到 PDF 与预期值。附注范围要显式列出，不能对未纳入的附注宣称全行覆盖。
- [ ] 原披露值规范化后精确相等；计算保留 Decimal，显示值按明确舍入规则相等；因披露舍入产生的勾稽误差使用逐规则登记的界限，不用统一百分比容差掩盖量级错误。
- [ ] 至少三家基线回归，以及至少两份冻结留出首跑；留出组合须包含一家新公司和一份新期间。分别记录自动解析、人工修正、最终结果和耗时，不得把人工修正后的通过写成全自动通过。
- [ ] 修复导致样本污染时按 P00 降级并补新留出；建立回归集，对所有范围内关键指标逐项核验；非可算指标明确原因。

验收：范围内预期行全部有状态，未解释的金额/身份错误为 0；缺失没有被零值掩盖；独立答案完整；至少三家基线与规定两份留出组合通过，结果和人工介入透明。失败可修复重验；收缩支持范围只能作为受限试验说明，不能自动豁免正式样本门槛或宣布阶段通过。不以漂亮图表替代。

### P06：客观事实报告与主观证据门禁分离（2–3 人日，P0）

来源 I18/I19/I27；对应 D05。负责人：后端 + 财务/产品复核。

文件：`services/api/src/flow_api/publishing/{models,service,publication}.py`、`services/api/src/flow_api/analysis/{policy,evidence}.py`、`services/api/src/flow_api/api/schemas/publishing.py`；新增 `services/api/tests/publishing/test_objective_eligibility.py`。

- [ ] 先测试：纯客观报告无 Finding 也可在事实复核通过后进入批准流程；存在未复核事实、勾稽阻断或证据拒绝时不能偷偷绕过。
- [ ] 定义客观报告资格合同：来源/事实版本完整、关键质量门禁、复核状态、允许范围及不可用项说明；与主观 Finding 证据审批分离。
- [ ] 新报告类型明确权限与状态转移，用户批准才能冻结；AI 建议不能替代批准。
- [ ] 旧月报和主观结论继续走原有证据门禁，回归 rejected evidence 与 approved finding 的边界，不因新类型放松旧规则。

验收：客观与主观两条状态矩阵均覆盖允许/禁止路径；无虚构 Finding；新类型和旧类型兼容；越权或跳过复核失败可解释。

### P07：四问分析工作台与可解释交互（3–4 人日，P1）

来源 I06–I16；对应 D01 UI 与 E06。负责人：前端 + 产品。

文件：复用 `apps/web/components/dashboard/metric-grid.tsx` 和现有页面入口；新增 `apps/web/components/objective-analysis/objective-analysis-workbench.tsx`、同目录样式、`apps/web/tests/components/objective-analysis.test.tsx`、`apps/web/e2e/objective-analysis.spec.ts`；使用现有合同生成机制。

- [ ] 先写组件与浏览器断言：有数据/缺数据/不可比/待复核四类状态；预算缺失不能展示完成率；点击指标可达口径和来源。
- [ ] 用四问组织默认阅读，六专题为展开视图；首屏显示范围/期间/币种/版本及少量关键结果，不复制宣传大屏。
- [ ] 实现筛选状态可见、清除与返回上层；禁止粒度的下钻按钮禁用并说明；比较模式显示所选预算/基期。
- [ ] 采用 KPI、趋势、结构、桥和明细的最小有用图形；负数、长名称、单位、中文、窄屏和键盘可用性纳入验收。
- [ ] 通过屏幕检查和无障碍检测，记录财务用户走查的问题、修改与复验。用户走查不能由开发截图自证代替。

验收：能从问题到证据完成实际任务，页面无自算指标、无伪造空图；axe 无 serious/critical；范围受限情况下清晰说明，不能以“暂无数据”掩盖解析错误。

### P08：统一冻结载荷和真实多格式报告（4–6 人日，P1）

来源 I17/I18/I20/I29；对应 E01–E04。负责人：后端/前端。

文件：`services/api/src/flow_api/publishing/{models,renderers,service}.py`、`services/api/src/flow_api/api/schemas/publishing.py`、`apps/web/components/reports/reports-center.tsx`；测试沿用 publishing golden 套件，新增 `services/api/tests/publishing/test_objective_renderers.py`；容器路径在 P00 定位后修改现有部署文件，不另起平行渲染服务。

- [ ] 先冻结 typed payload 合同：`report_type`、`schema_version`、源/快照/口径版本、报表行、指标、图形数据、证据、不可用原因、批准记录。复用原不可变 JSONB，不使用任意自由字典绕过验证。
- [ ] 保存冻结时全部分析与显示输入；后续修改词典/来源/当前窗口，旧 payload 和已生成产物哈希不变。
- [ ] HTML/XLSX/PDF 从该载荷渲染；XLSX 带口径与证据页；PDF 由实际 Chromium 打印链路产出，验证中文字体、分页、表格跨页、负数和页码。
- [ ] 新客观报告本阶段必交 HTML、XLSX、PDF。已有 PPTX 功能必须回归保留；新增客观报告 PPTX 不计入本轮默认交付，若需要须确认范围与追加验收，不静默假装支持。
- [ ] golden 核验所有范围数值与文字，不只存在文件；PDF 实际打开/逐页渲染检查，XLSX 独立读取单元格，图表数据与表格一致。

验收：三格式范围数值一致、版式可阅读；真 PDF 非 HTML 改后缀；旧报告不受新版本影响；下载文件名、权限、内容类型和哈希正确。字体/打印容器不可用则报告该格式阻塞，不能伪通过。

### P09：真实存储完整用户旅程与旧缺陷回归（2–4 人日，P0/P1）

来源 I20/I27–I29；对应 E05 和既有修复回归。负责人：测试/全栈。

文件：新增 `apps/web/e2e/objective-report-journey.spec.ts`；复用 API integration 与浏览器已有上传/发布/报告测试；证据写入 `docs/implementation/objective-analysis/next-stage-journey.md`。

- [ ] 使用真实测试 S3/MinIO 和隔离数据库，从浏览器上传正式样本，校准映射/期间，确认质量问题，完成复核、计算、分析、批准、冻结、下载；不以存储替身作为最终验收。
- [ ] 失败矩阵包含上传中断、抽取失败、重试幂等、未经授权下载、过期会话、跨批次证据、指标版本变动；用户能恢复或获得清晰阻断原因。
- [ ] 将 R1–R9/N1–N3 的教训逐项映射现有回归测试，包含冻结漂移、拒绝证据、映射持久化、SHA422、质量确认、审计事务、跨批次 latest、PPT 正文尺寸、下载名称、认证代理、合同漂移和测试隔离。
- [ ] 回归已修 S3 代理、CI 进程清理与导航对比度，不重新宣称它们是当前已知故障。记录环境配置和产物哈希，日志脱敏。

验收：主旅程及失败路径自动测试通过；真实对象可读回并核对哈希；没有 mock-only 的成功声明；测试任务只清理自身资源，不终止无关用户进程。

### P10：发布运行保障与恢复演练（3–5 人日，P0）

来源 I29；对应 F01–F05。负责人：平台/运维。

文件：读取并按需增量修改现有 compose、容器、CI 配置；新增 `docs/runbooks/objective-analysis-release.md`、`docs/implementation/objective-analysis/restore-drill.md`。准确部署文件由 P00 基线清单登记，禁止假定新环境已存在。

- [ ] 检查真实容器下 API/worker/打印器/字体/对象存储，列必需密钥、网络、健康检查和最小访问权限，密钥不入库。
- [ ] 在隔离环境备份并恢复数据库与对象；验证源文件、冻结载荷及报告文件哈希，数据库只恢复成功但对象丢失不算通过。
- [ ] 记录恢复耗时与恢复点；提出 RTO/RPO 目标供业务确认，未经确认不宣传 SLA。
- [ ] 在指定获授权环境验证 HTTPS、身份/权限、日志关联身份与敏感信息脱敏、任务重试、回滚步骤。未指定部署环境时本项阻塞，不自行创建付费资源或公开服务。
- [ ] 全部回归门禁接入现有 CI，审阅实际结果而非只看提交状态；测试依赖就绪失败明确暴露，不跳过关键用例后宣称绿色。

验收：可复现部署/回滚和完整恢复记录；密钥及财务数据不泄漏；范围内最新提交 CI 全绿。此任务未来执行也不包含未获授权的采购、外部接入或生产破坏性操作。

### P11：阶段总验收、使用文档与范围冻结（2–3 人日，P0）

来源 I30；对应 F06、E06。负责人：技术 + 财务 + 产品联合确认。

文件：新增 `docs/implementation/objective-analysis/next-stage-acceptance.md`；更新当前状态、用户操作文档、总计划相关完成证据和知识库索引/校验清单。

- [ ] 按第四节验收矩阵收齐原始结果、CI 链接、样本版本、支持范围、失败案例和未解决项；不得仅引用本计划说“已经完成”。
- [ ] 与总册 I01–I30/C01–C18 对账，给出已落地/已复用/不适用/延期及原因，每项指向证据或后续决策。
- [ ] 财务用户完成从上传到证据与报告的走查，记录问题与复验；说明支持的公司/格式/期间、人工步骤及不支持的内部细分。
- [ ] 更新唯一当前状态，保留旧验收历史；刷新知识库索引和哈希，按任务提交推送。
- [ ] 输出 Go/No-Go 建议，用户或正式负责人确认后才进入下一阶段；有阻断缺陷时维持受限试验，不以开发完成代替业务验收。

验收：所有 P0 出口通过、P1 承诺范围完成；至少三家基线和两份留出（含一家新公司、一份新期间）满足 P05；没有未说明的准确性缺陷和验收占位项；延期不被标为完成；公开发布和内部试点分别有范围确认。

### P12：企业内部数据与高阶经营分析储备（本阶段仅保留路线）

来源 I13/I21–I27；对应 G01–G03/H01。不计入 P00–P11 完成率，不在本轮启动。

候选文件：未来新增 `docs/superpowers/specs/internal-operations-pilot-contract.md`、授权数据字段清单、独立验收样本；优先复用现有经营轨配置，不先建新运行服务。

- [ ] G1 准入：P11 通过，取得数据所有者授权、脱敏/存储边界、责任人和验收指标。机密企业原始数据不能提交公开仓库。
- [ ] G2 先选一个业务闭环（建议收入—应收—回款，最终由用户和可用数据决定），建立实体/经营组织/人事组织、有效期与客户/订单主键，规范共享指标口径。
- [ ] G3 先用合成数据验证重复联接、预算粒度、组织变更和权限，再用获授权样本做独立对账；合成通过不能冒充真实业务验收。
- [ ] G4 单闭环通过后再选择费用预算、库存供应或交付效率，不一次铺满八条业务线；OEE 等只有行业和数据适用才纳入。
- [ ] H1 预测/情景须另有历史训练与留出回测、误差标准及人工审批；13 周现金计划不能由历史图表自动推导。
- [ ] H2 因果/行动闭环需事件证据、假设状态、责任确认、执行日志、反事实或可接受的效果评价方法；AI 不自动派发外部任务或认定收益。

出口：独立立项规格、授权、预算与样本齐备后另排详细实施任务。没有这些前提，保持储备，不借“升级改造”扩大执行权限。

## 4. 统一验收矩阵与执行时验证命令

| 维度 | 必达条件 | 证据 | 阻断情况 |
|---|---|---|---|
| 来源 | 每个范围内事实有身份/定位/版本 | 源哈希、行级链接、复核记录 | 定位缺失却标已复核 |
| 口径 | C01–C18 逐条处置，法定/管理清楚 | 订正差异、官方条款、版本历史 | MPM 等误标仍传播到新报告 |
| 计算 | 独立答案与显示舍入符合合同 | 全行 diff、指标 oracle、守恒测试 | 未解释金额/身份错误 |
| 泛化 | 至少三家基线 + 两份未污染留出，含一家新公司、一份新期间 | 污染记录、自动/人工首跑结果 | 把适配样本当新留出，或收缩范围后豁免组合门槛 |
| 界面 | 四问到证据、可用性清晰 | 浏览器任务、axe、人工走查 | 隐藏范围、演示数混入 |
| 报告 | 冻结不变、三格式相符、旧格式无退化 | payload/文件哈希、PDF/XLSX 检查 | 假 PDF、旧快照漂移 |
| 运行 | 真实存储、恢复、授权部署与 CI | 真实旅程、恢复记录、最新 CI | mock-only、对象未恢复 |

以下仅供**未来执行**，本次不运行应用测试或启动基础设施：

```sh
make contracts-check
make test-api
make test-web
make lint
make typecheck
make test-metrics-known-answers
make test-analysis-invariants
make test-dashboard
make test-copilot-evals
make test-publishing-golden
make acceptance
```

执行前读 Makefile 和环境要求，在隔离测试资源中运行；现有命令不会自动涵盖未来新增用例，每个 P 任务必须将新增测试纳入相应套件。P05 新验证脚本约定 `--manifest validation/financial_reports/manifest.yaml --fail-on-unexplained-diff`，须先实现 CLI 合同并测试，不能在脚本不存在时报告已运行。浏览器用例通过现有 Playwright 配置执行，记录浏览器/服务器/存储版本。任何 skip 或缺依赖必须进入验收例外，不能省略。

## 5. 变更、迁移、回滚与风险控制

| 风险 | 控制 | 所属任务 |
|---|---|---|
| 同义指标重复、名称变化破坏引用 | 稳定 metric_code、别名、版本迁移，旧引用可读 | P01 |
| 订正让历史报告数值漂移 | 新版本激活，不就地重算旧快照；明确可申请重算产生新报告 | P01/P08 |
| 新报告类型绕过旧审批 | 分类型资格合同与权限状态矩阵，旧路径负例回归 | P06/P09 |
| 全局粒度/期间改动影响预算与同比 | 先列消费者，绑定明确身份与兼容规则 | P02/P04 |
| 测试答案与实现同源 | 原 PDF 独立录入，单独复核，锁定后运行 | P00/P05 |
| 屏幕丰富但数据不够 | 可用性驱动展示，缺失解释，不补假值 | P02/P07 |
| 迁移风险 | 优先兼容增量；新字段 unknown 而非伪造已核验；先备份和恢复演练 | P01/P10 |
| 新旧界面或服务切换 | 若需灰度，使用明确版本/开关；回退到上一兼容应用版本，保留新数据和审计，不删历史 | P08/P10 |
| 资料版权/隐私 | 只保留许可允许的摘要和链接，内部数据授权脱敏，机密不公开提交 | P00/P12 |
| 并行工作树冲突 | 开始前划分文件所有权，遇同文件冲突暂停协调，不覆盖用户修改 | 全部 |

数据库迁移编号以实际执行时 head 为准，不预占 0019；不能回滚到读不了新数据的版本。必须先验证前向/向后兼容和报告可读性，再允许发布切换。资源、部署目标、SLA 与企业数据权限需各自确认，本计划不提供这些授权。

## 6. 与旧计划和资料建议的映射

| 原阶段/建议 | 本计划安排 | 处理方式 |
|---|---|---|
| A01–A03、B01–B06 | P00/P04/P05 | 已有底座复用，校准状态并补增量/验收 |
| C01–C06 | P01，相关 P00/P02 | 不重复建设；修正语义与覆盖证据 |
| D01/D02 | P02/P03/P04/P07 | 四问六专题、比较和同源图形 |
| D03/D04 | P00 + P05 | 将独立答案和留出准备提前 |
| D05 | P06 | 客观报告资格与主观证据分开 |
| E01–E04 | P08 | typed 载荷、HTML/XLSX/真 PDF、golden |
| E05/E06 | P07/P09/P11 | 完整旅程和使用验证 |
| F01–F06 | P10/P11 | 运行、恢复、发布验收 |
| G01–G03/H01 | P12 | 保留授权门槛，未来单独实施 |
| 十指标 U1–U8 | I06–I13 → P01/P02/P03/P07/P12 | U3 改为先复用既有净现比 |
| FineBI 六专题/比较/下钻/共享快照 | I14–I17 → P02/P03/P04/P07/P08 | 不照抄未查看的模板包 |
| 全部其他参考与优化 I01–I30 | 总册第六节逐条映射 | 每项有任务或明确延期出口 |

## 7. 本次计划任务的交付边界

本次交付总册、这份详细计划及必要索引/状态说明/校验清单更新；检查文档完整性、路径、来源覆盖与计划一致性，进行一次独立计划审阅，然后只提交这些文档并推送。应用、数据库、配置、运行环境和其他任务的界面修改均不属于本次交付。

文档提交成功只代表规划已交付，不代表 P00–P12 已执行、错误已修好、最新工程 CI 已通过或下一阶段已获批准。

### 文档审阅与并行状态记录

2026-09-07 独立计划审阅结论：Approved。初审提出留出样本门槛不够明确，现已在 P00/P05/P11 和验收矩阵统一为至少三家基线、另两份留出且含一家新公司与一份新期间；同时明确累计差分为现行合同下的拒绝性反例。

交付检查时观察到其他任务另提交 `0554dca` 与 `751169f`，后者校准了部分旧计划完成标记。本文保留 `8c7dbf3` 作为研究读取时点，不将其他任务的新实现计为本次交付，也不据旧复选框重复排工。执行时 P00 须以届时实际 HEAD 更新；工作区新增 `config/analysis/` 等不在本次提交范围。
