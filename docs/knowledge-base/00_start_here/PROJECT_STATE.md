# FLOW 当前项目状态｜2026-09-12

状态：当前入口。O2 已由 `293308c` 完成，O3 已由 `2dc88c7` 完成并进入 `main`；其后 `8f7f5bc` 修复 MinIO 注册表。指标库 v1.1 已由 `3d4f92d` 交付，`9f00cc9` 修复治理测试读取最新字典；当前 HEAD `9f00cc9` 的 GitHub Actions run `34676253964` 成功。数据库迁移头为 `0024_operations_publication`。U 系列推进以[统一计划](../../superpowers/plans/2026-09-07-unified-next-plan.md)为唯一入口，经营轨以 [O 系列计划](../../superpowers/plans/2026-09-09-operations-track-plan.md)记录断点。本页取代此前同文件的当前状态摘要，完整时间线保存在[历史状态快照](2026-09-07-project-state-history.md)，不再把历史“下一步”当今日待办。

## 1. 当前方向与计划关系

[D049 正式方向](../../superpowers/specs/2026-09-06-objective-financial-analysis-direction.md)有效：以公开财报建设可追溯、可复核的客观财务分析；企业内部数据是最终主场景但另行试点。顺序为客观事实与计算、证据支持的推断、改善行动。保留双轨底座，不建设总账，不伪造缺失事实，不将数学贡献当成因果。

- **[统一执行计划 U1–U10](../../superpowers/plans/2026-09-07-unified-next-plan.md)：2026-09-07 起唯一执行入口**（用户指令：M/P 两系列全部归档并入；任务领取/勾选/提交只用 U 编号，映射：P03=U2、P04=U3、P05=U4、P06=U5、P08/P09=U6、P07=U7、P10/P11+F=U8、P12+G=U9、H01=U10；每完成一个阶段提交推送、CI 绿才算 done）。
- [机器交接：执行计划与两机协作协议（2026-09-07）](../07_handoff/2026-09-07-machine-handoff-p03-plan.md)：跨机器操作的信息交换载体——交接快照、接下来要执行的任务书（P03/U2 起含登记缺陷处置）、两机协作纪律与可迁移环境经验。
- [P 系列任务书（已归档）](../../superpowers/plans/2026-09-07-next-stage-upgrade-plan.md)与 [M 系列台账（已归档）](../../superpowers/plans/2026-09-06-objective-financial-analysis-master-plan.md)：保留任务细节与历史证据，不再更新。
- [参考与订正总册](../02_research/synthesis/2026-09-07-reference-and-improvement-master.md)：30 项建议、18 项订正及 417 条素材索引；不是额外产品需求批准。

## 2. 已实现和应复用的能力

| 能力 | 最新已提交证据 | 边界 |
|---|---|---|
| 既有物流窄切片 | Phase 1–10、数据工作台、证据调查、Copilot、冻结报告 | 历史功能验收不等于生产就绪 |
| 来源与财务事实 | B01–B05：文本 PDF 来源登记、适配抽取、版本化归一、勾稽、复核 | 不承诺任意 PDF；独立全行答案和留出仍须补验 |
| 发布后构建 | B06：按批次期间构建快照/分析，持久 BuildJob、重试查询接口 | API 同步构建不等于 Celery 异步执行；完整浏览器新财报旅程另验 |
| 指标知识与治理 | C01–C06：定义、绑定、语义、草稿/验证/激活/退役、审计、影响沙箱和界面 | C01 既有字段审计不能证明所有会计分类正确，订正另列 |
| 配置规模 | 指标库 v1.1 共 64 条（常用 32/专业 32）、167 科目、48 准则登记、32 分录模板；经营轨 43 指标/6 域 | 64 条含物流行业集；登记、绑定、事实可算、展示覆盖应分开统计 |
| 界面 | `/data`、`/statements`、`/metric-library`、`/reports`、调查、四问工作台与双轨导航 | `/operations` 已接真实六主题概览、严格期间事实、冻结及 XLSX/PPTX/HTML/PDF 导出 |
| 真实对象存储 | 2026-09-06 S3 系统代理根因修复及真实上传/发布/下载记录 | 历史 PutObject 超时不是当前未修故障；目标部署回归仍需做 |
| 报告中心 | 客观报告与经营报告的不可变载荷、多格式渲染、追加式发布尝试、对象存储和统一下载 | PDF 依赖部署环境中的固定 Chromium；缺失打印器必须显式 failed，不得假成功 |
| 认证 | 单用户密码、会话、服务端 Bearer 与同源代理 | 无企业多角色/多租户/SSO 保证 |

## 3. 本次读取的验证状态

截至本轮登记：O3 提交 `2dc88c7` 已推送且为当前 `main` 的祖先；其最初 CI 因 Docker Hub 下架 `minio/minio:latest` 导致多个基础设施 job 同源失败，后续 `8f7f5bc` 切换到官方 Quay 镜像后恢复。指标库 v1.1（64 条、分层、分析维度、依赖图）由 `3d4f92d` 交付；首轮 CI 暴露治理测试固定导入旧 YAML，`9f00cc9` 改为解析最新字典后，run `34676253964` 成功。O3 本地验证包括后端 183 项受影响测试、前端 52 项组件测试、类型/静态检查、契约与迁移往返、Next production build，以及 PostgreSQL/MinIO 真实发布下载烟测。迁移检查确认唯一 head 为 `0024_operations_publication`。本地生产栈是否正在运行属于瞬时运行状态，不以历史交接中的“当前健康”代替实时检查。

2026-09-07 的文档治理轮（`de3a672`、`22061da`）与改名/状态校准轮只变更文档，不重跑应用全套、不启动基础设施、不作生产认证。

## 4. 尚未完成与下一步（2026-09-12：U8 进行中；U4 等外部输入；U9/O5 待授权）

**近期完成证据（均非当前待办）**

- **U5/U6/U7 done**：客观报告资格门禁、统一冻结与 HTML/XLSX/PDF/PPTX 输出、四问工作台及可解释交互均已交付；对应提交与测试证据见[统一计划](../../superpowers/plans/2026-09-07-unified-next-plan.md)。
- **O2/O3/O4 done**：严格期间经营事实、六主题多格式发布与 `/operations` 看板均已交付；迁移头为 `0024_operations_publication`。
- **指标库 v1.1 done**：64 条指标、常用/专业分层、分析维度和依赖图已交付；当前 CI 证据为 run `34676253964` 成功。

**当前未完成工作（按依赖领取）**

1. **U8 当前主线**：收口真实存储完整旅程、HTTPS 部署拓扑、结构化日志和最终统一部署验收；备份恢复已 done。
2. **U4 外部到料即独立并行**：由未参与抽取器开发的独立会话录入 oracle，完成全行 diff 与留出泛化验收。
3. **`rnd_exp` 官方核验**：取得《应用指南汇编 2024》原文后复核暂登记 4301；不阻塞 U8。
4. **U9/O5 授权后试点**：硬依赖明确的数据授权与 U8 安全/部署收口；验证主数据、版本身份、多维盈利守恒、窄主题分析包及“一报一会”记录闭环。
5. **U10 证据决策**：硬依赖 U4 + U8 + U9/O5，逐项决定 V1.1 的 go/hold/drop。

**统一执行计划 U1–U10 是唯一执行入口**（M/P 两系列已归档并入；任务领取/勾选/提交只用 U 编号；每完成一个阶段提交推送、CI 绿才算 done）。跨机器协作与 U2 任务书细节见[交接文档](../07_handoff/2026-09-07-machine-handoff-p03-plan.md)（§2.2 已含完成登记）。

不再列为待办：重做 Phase 1、重建指标库、逐项审批 v0 草案、修复已处理的 S3 代理问题、再造已有净现比、重做 P00–P02 与 U1–U2 已交付内容。FLOW 名称已按 D044 保留；英文释义/品牌完善不阻塞当前客观分析主线。

## 5. 更新纪律

每次只依据提交、测试和用户确认更新状态；历史失败与已修证据保留。当前页保持简短，历史时间线不再叠加到当前摘要。修改正式合同先查决策与影响图；实施需对应任务授权。知识库更新同步索引及哈希，完整任务仅提交范围内文件并推送 origin。

**Obsidian 内部知识基线（D051）**：默认固定引用 `obsidian-2026-09-12T15:46+08:00`，财经/经营预设范围覆盖 3035 篇正文；首轮 2188 篇的 Agent 订正分级为 HIGH 1026、MED 566、LOW 596，微信增量 847 篇完成程序全文主题路由与代表篇精读。以后每次计划/功能/更新先执行统一计划 KG-1–KG-5，引用该基线并记录采用/拒绝理由；只有用户明确要求或单独启动知识库维护时才建立新截面并读取增量。最新素材把 U9/O5 细化为主数据与版本合同、多维盈利守恒、分析包和一报一会闭环，但不改变 U8 当前主线、U4 外部到料独立并行、U9/O5 依赖授权与 U8、U10 硬依赖四者收口的顺序。

## 6. 2026-09-08 Code review 与修复轮（历史证据，`fa472f4`）

三路 review（计划/代码质量/工程健康）后的修复已提交推送：

- **P0**：业绩公告类缺表报告发布不再 500（两个勾稽器缺表守卫）；核心报表缺失同样阻断发布（B04 门禁补漏）。
- **P1 数据正确性**：净现比去掉 abs()（符号如实）；腾讯括号负数无空格不再丢负号；复核视图保持 Decimal（不经 float）；0 值 `or` 回退改显式 None 判断。
- **双源一致**：执行绑定读 v1 映射（含 {item,abs} 目标）；沙盒试算支持 sum 算子；负分母政策与 C03 统一（不伪造比率）；指标库 DB 载荷不再丢 known_gaps/categories/取代说明。
- **工程**：编排幂等键加目录+政策指纹（迁移 0021）；崩溃残留的 running 构建任务被回收；Dockerfile 打包全量 config/（容器内编排/归一化/影响/客观分析可用）。
- 回归：statements/analysis/metrics 176 + orchestration 5 + api/integration 20 通过；mypy/ruff/契约/迁移往返全过。
- 当时遗留项的现行处置：渲染器 API 接线与客观 PDF 发布链签名已随 U6/O3 关闭；治理表数据库并发唯一约束、指标引用递归循环保护仍须在相关合同变更前重新验证；U8 只承接部署环境中的 PDF/Chromium、真实存储和运行日志验收。该历史段不作为当前领取清单。

## 7. 2026-09-10 默认分析主体导入：阿里巴巴 + 菜鸟（用户指定）

Web 驾驶舱空白的根因是库内业务表 0 行（U8 备份演练后未回灌）。按用户指令，以阿里巴巴/菜鸟为默认分析主体导入全部可得公开数据：

- **源件**（p5_samples，含来源+SHA-256）：阿里 6 份年度业绩公告（HKEX 官方，FY2019–FY2026 覆盖）+ 菜鸟招股书申请版本（666 页，2024-03 撤回，附录 I 会计师报告 IFRS）。
- **抽取**：`scripts/p5_extract_cainiao.py`（FY21-23 三表，42/42 勾稽绿）与 `scripts/p5_extract_alibaba.py`（8 财年三表，108/108 勾稽绿）+ `cainiao_segment_series.yaml`（菜鸟分部收入 FY2019–FY2025、经调整 EBITA FY2023–FY2025；FY2026 起阿里不再单独披露）。
- **入库**：别名映射表新增 `cainiao_private`/`alibaba_9988` 节 + 归一化公司键；11 份报告（菜鸟 FY2021–FY2023 + 阿里 FY2019–FY2026）导入→归一化→发布→客观快照冻结（`objective.v1`），statements 74 测试通过，黄金值与 PDF 原文逐项一致。
- **基础种子回灌**：会计基础+指标字典 v1（55/167/48/32，与审计基线一致）。
- **边界（诚实声明）**：经营驾驶舱的 dim/fact 月度模型不喂年度公开数据（摊月=造假）；其真实数据接线属 O2（季度级），菜鸟分部序列 YAML 已备好作输入。阿里 US GAAP/菜鸟 IFRS 行项目仅映射主干 item_id，明细行如实 item_id=None；菜鸟流动借款行未抽取（前缀碰撞，已在样本 README 注明）。
