# FLOW 可复用接续提示词｜2026-09-07

状态：当前提示词；使用者选择与当前授权相符的一条，不把本文件当作自动执行指令。

## 只理解与评审

```text
先读 docs/knowledge-base/00_start_here/AGENT_START_HERE.md、PROJECT_STATE.md、D049 方向及文档登记。对照实际 HEAD 和 CI，说明当前有效方向、已实现和待验收能力、在途修改以及下一阶段依赖。只评审，不实施；每项结论给文件或提交证据，不把历史测试当本次通过。
```

## 获准后继续下一阶段

```text
按照 docs/superpowers/plans/2026-09-07-next-stage-upgrade-plan.md 从 P00 刷新基线，再按依赖实施获授权范围。A01–H01 总计划作为里程碑与历史证据台账，不重复建设已有 B/C 能力。独立答案与未污染留出先准备，净现比先复用，语义订正须版本化。发现并行修改先协调。按 AGENTS.md 验证、更新文档清单并只提交任务范围文件、推送 origin。
```

## 只核查部署缺口

```text
先读最新项目状态、运行架构和认证说明，再核对目标环境。区分已有 S3 代理修复和真实链路历史记录，与本次容器/PDF/HTTPS/恢复/日志待验证项。仅诊断，不运行破坏性验收、不购买资源、不部署。报告证据、风险、授权依赖及补验建议。
```

## 核查原始依据或修改正式决定

```text
先在 DECISION_LOG.md 和 CHANGE_IMPACT_MAP.md 定位议题，通过参考订正总册追到原始来源。区分外部建议、AI 总结、用户已确认决策和当前实现；说明拟改范围与影响，未经确认不变更正式契约。原件不可覆盖，修正另增说明。
```
